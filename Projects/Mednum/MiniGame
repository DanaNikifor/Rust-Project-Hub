Мини игра угадай, 'что я загадал?' на Rust:
```Rust
use std::io; // Импортируем модуль io для работы с вводом/выводом
use rand::Rng; // Импортируем модуль rand для генерации случайных чисел

fn main() {
    let secret_number = rand::thread_rng().gen_range(1, 11); // Генерируем случайное число от 1 до 10
    let mut guess = String::new(); // Создаем переменную для хранения введенного пользователем числа
    let mut attempts = 0; // Создаем переменную для отслеживания количества попыток

    println!("Угадай число от 1 до 10. У тебя 3 попытки."); // Выводим инструкцию на экран

    while attempts < 3 { // Запускаем цикл, который будет продолжаться, пока пользователь не угадает число или не исчерпает все попытки
        io::stdin().read_line(&mut guess).expect("Failed to read line"); // Считываем введенное пользователем число
        let guess: u32 = guess.trim().parse().expect("Please type a number!"); // Преобразуем введенное пользователем число в число типа u32

        if guess == secret_number { // Если пользователь угадал число
            println!("Поздравляю! Ты угадал число!"); // Выводим поздравление на экран
            break; // Завершаем игру
        } else { // Если пользователь не угадал число
            println!("Ты не угадал. Попробуй еще раз."); // Выводим подсказку на экран
            attempts += 1; // Увеличиваем количество попыток на 1
        }
    }

    if attempts == 3 { // Если пользователь исчерпал все попытки
        println!("Ты проиграл. Загаданное число было: {}", secret_number); // Выводим сообщение о проигрыше и загаданное число на экран
    }
}
```